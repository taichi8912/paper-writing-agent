\section{Introduction}
Static analysis tools slow down on large codebases. We train a CNN on token
sequences and evaluate it on a public benchmark of source files. We compare
against a rule-based baseline and report accuracy, inference latency, and memory
use. The window was 4{,}096 tokens wide.

% This file, read after abstract.tex, uses CNN bare; the term is defined there.
